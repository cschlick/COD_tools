from distutils.core import setup

setup(
    name='CODTools',
    version='0.1dev',
    packages=['cod_tools'],
    install_requires=[],
    license="",
    long_description=open('README.md').read(),
)